# Project ORBIT — development and test dependencies
#
# Kept separate from requirements.txt so these are never installed into Cloud
# Composer. The test suite mocks the RAG and GCS boundaries, so it needs only the
# API stack, not langchain/chromadb/sentence-transformers.
#
#   pip install -r requirements-dev.txt
#   pytest tests/ -v

pytest==8.3.3

# Needed to import and exercise src/backend/api.py in tests.
fastapi==0.115.0
pydantic==2.9.2
httpx==0.27.2                    # required by fastapi.testclient
google-cloud-storage==2.18.2     # api.py imports this lazily, but tests touch the path
